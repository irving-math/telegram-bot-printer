# Telegram bot for print in Docker

This repo contains a telegram bot for print  tasks, this is usefull for printers that does'nt have Wi-Fi connection.

## Init

```bash
# Build the container
./build.sh

# Run the container
./run.sh
```
